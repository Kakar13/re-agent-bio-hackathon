# Copy to .env and fill in keys from the hackathon Discord / lightning talks.
#   cp .env.example .env

# Lets `uv run python` import src/re_agent
PYTHONPATH=src

# Anthropic — Claude API credits from the event (also used by Pi)
ANTHROPIC_API_KEY=

# LangSmith — Pi session traces (turns, tools, tokens)
# Key: https://docs.langchain.com/langsmith/create-account-api-key
LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=reAgent-hackathon
# Pi extension specifically checks TRACE_TO_LANGSMITH; ./run.sh maps
# LANGSMITH_TRACING → TRACE_TO_LANGSMITH automatically.
# TRACE_TO_LANGSMITH=true

# Proto (Arc Institute) — hosted MCP + proto-client SDK
# Get a key from your Proto workspace, then: export PROTO_API_KEY=...
PROTO_API_KEY=

# Hugging Face — only needed for gated Proto models (ESM3, AlphaFold3, AlphaGenome)
HF_TOKEN=

# Modal — preferred auth is `uv run modal setup` (writes ~/.modal.toml).
# Env tokens below are optional fallback only; do not invent them.
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=

# Benchling — candidate / run system of record (booth + Discord)
# Never commit real keys. Claim access at the Benchling booth.
BENCHLING_TENANT_URL=
BENCHLING_API_KEY=
BENCHLING_FOLDER_ID=
# Optional: custom entity schema for run records; without it we use reduced notebook entry mode
BENCHLING_RUN_SCHEMA_ID=

# Optional licensed DTU predictors (user-provided academic binaries — do not redistribute)
# NETMHCPAN_BIN=/path/to/netMHCpan
# NETMHCIIPAN_BIN=/path/to/netMHCIIpan

# Offline demo without MHCflurry models (CI / first boot only — clearly labeled heuristic)
# IMMUNO_ALLOW_HEURISTIC_MHC=1

# Optional local overrides
# PROTO_HOME=~/.proto
# PROTO_MODEL_CACHE=~/.proto/weights
